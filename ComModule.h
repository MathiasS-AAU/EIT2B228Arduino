/*#include <string>

class ComModule
{
private:
    // data
    std::string GCODES [5];
    bool controlState;
    float lastPos [2];
    float currentPos [2];
    float nextPos [2];

public:
    ComModule();
    ~ComModule();
    bool sendCode();
    bool connect();
};/

*/
